# Базовая линия проекта «Помогай»

## Назначение документа

Этот документ фиксирует восстанавливаемую исходную версию проекта «Помогай» (Pomogay / Helpinrus) перед началом дальнейшей разработки.

## Основные сведения

- Проект: «Помогай» / Pomogay / Helpinrus
- Дата начала фиксации: 2026-08-24
- Дата создания baseline tag: 2026-08-25
- Публичный сайт: https://201.51.4.212
- GitHub: https://github.com/iPlusmsk07/helpinrus
- Production branch: `main`
- Production commit SHA: `f89c0fe7d0f940edbecbb36232d4bb5c78f9a728`
- Baseline tag: `baseline-pre-implementation-2026-08-24`

## Резервные копии

- Исходный архив: `Помогай_v0.9.3_PROFILE_ARCHITECTURE.zip`
- SHA-256 исходного архива: `3c8cabb58c8bf5edc1861e59d853339e6bb9b39e3e8d2e1aff52eba7508dc658`
- Создана отдельная копия исходного архива.
- Создана ZIP-копия GitHub-репозитория на production commit.
- Обе ZIP-копии проверены: ошибок в сжатых данных не обнаружено.

## Проверенные ключевые файлы

Содержимое каждого файла было побайтно сопоставлено между исходным архивом, GitHub commit и опубликованным production-релизом:

- `index.html` — совпадает во всех трёх источниках;
- `app.js` — совпадает во всех трёх источниках;
- `styles.css` — совпадает во всех трёх источниках;
- `config.js` — совпадает во всех трёх источниках;
- `sw.js` — совпадает во всех трёх источниках;
- `manifest.webmanifest` — совпадает во всех трёх источниках;
- `package.json` — совпадает во всех трёх источниках.

## Найденные различия

По проверенным ключевым файлам различий между архивом, GitHub и production не найдено.

## Подтверждённая схема публикации

- Связанный репозиторий: `github.com/iPlusmsk07/helpinrus`
- Production branch: `main`
- Production-сервер: `201.51.4.212`
- Исходники на сервере: `/opt/helpinrus`
- Публичный путь: `/var/www/helpinrus`
- Сборка: `node scripts/prepare-www.mjs`
- Публикация выполняется из `main` повторяемым deploy-скриптом после успешных тестов.

## Неизвестные сведения и ограничения проверки

- Проверялись только перечисленные ключевые файлы; полная функциональная эквивалентность всех файлов и внешних сервисов не утверждается.
- Supabase в рамках фиксации не изменялся и отдельно не тестировался.
- Реальная процедура отката не выполнялась; документирован только безопасный dry run.

## Ограничения текущей версии

Текущая версия является frontend-прототипом. Часть поведения использует `localStorage`, демонстрационные данные и частичную интеграцию Supabase.

Эту версию нельзя использовать для реальных платежей, хранения паспортных данных, результатов проверки личности, иной чувствительной информации или других операций с реальными пользовательскими данными без отдельного аудита безопасности и полноценной серверной реализации.
